info:
  name: delete_device_ecosystem_state
  type: http
  seq: 5

http:
  method: delete
  url: "{{base_url}}/api/device/ecosystem_state"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
