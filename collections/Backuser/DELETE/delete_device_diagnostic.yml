info:
  name: delete_device_diagnostic
  type: http
  seq: 3

http:
  method: delete
  url: "{{base_url}}/api/device/diagnostic"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​