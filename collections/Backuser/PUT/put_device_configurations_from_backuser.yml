info:
  name: put_device_configurations_from_device_operation_supervisor
  type: http
  seq: 2

http:
  method: PUT
  url: "{{base_url}}/api/device_operation_supervisor/device_configurations"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
