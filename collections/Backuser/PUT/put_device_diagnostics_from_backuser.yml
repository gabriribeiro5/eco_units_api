info:
  name: put_device_diagnostics_from_device_operation_supervisor
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base_url}}/api/device_operation_supervisor/device_diagnostic/{{diagnostic_id}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
