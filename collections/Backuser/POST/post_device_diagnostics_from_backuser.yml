info:
  name: post_device_diagnostics_from_backuser
  type: http
  seq: 10

http:
  method: POST
  url: "{{base_url}}/api/backuser/device_diagnostics"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
