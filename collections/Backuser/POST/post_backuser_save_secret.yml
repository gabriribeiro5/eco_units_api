info:
  name: post_backuser_save_secret
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/api/backuser/save_secret"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
