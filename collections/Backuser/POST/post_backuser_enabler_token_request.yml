info:
  name: post_backuser_enabler_token_request
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/api/backuser/enabler_token_request"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
