info:
  name: post_backuser_request_secret_reset
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/api/backuser/request_secret_reset"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
