info:
  name: post_device
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/device"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
