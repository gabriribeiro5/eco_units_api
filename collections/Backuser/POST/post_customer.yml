info:
  name: post_customer
  type: http
  seq: 9

http:
  method: POST
  url: "{{base_url}}/api/customer"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
