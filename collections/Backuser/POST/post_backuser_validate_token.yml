info:
  name: post_backuser_validate_token
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/api/backuser/validate_token"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​