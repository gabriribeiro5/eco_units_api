info:
  name: post_backuser
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/api/backuser"
  body:
    type: json
    data: |-
      {
        "agent_name": "Gabriel",
        "agent_email": "gabriel@email.com",
        "agent_secret": "some_secret",
        "confirmation_secret": "some_secret"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
