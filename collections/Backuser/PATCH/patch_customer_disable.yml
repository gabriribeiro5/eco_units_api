info:
  name: patch_customer_disable
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{base_url}}/api/customer/disable"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
