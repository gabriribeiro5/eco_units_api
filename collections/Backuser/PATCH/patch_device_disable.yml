info:
  name: patch_device_disable
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{base_url}}/api/device/disable"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
