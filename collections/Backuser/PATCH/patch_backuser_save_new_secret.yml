info:
  name: patch_backuser_save_new_secret
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{base_url}}/api/backuser/save_new_secret"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
