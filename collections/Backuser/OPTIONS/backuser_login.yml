info:
  name: device_operation_supervisor_login
  type: http
  seq: 3

http:
  method: OPTIONS
  url: "{{base_url}}/api"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
