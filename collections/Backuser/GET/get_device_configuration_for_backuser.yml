info:
  name: get_device_configuration_for_device_operation_supervisor
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/device_operation_supervisor/device_configurations"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
