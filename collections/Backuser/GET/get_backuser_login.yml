info:
  name: get_device_operation_supervisor_login
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/device_operation_supervisor/login"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
