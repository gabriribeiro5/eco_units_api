info:
  name: get_device_ecosystem_states_for_backuser
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/backuser/device_ecosystem_states"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
