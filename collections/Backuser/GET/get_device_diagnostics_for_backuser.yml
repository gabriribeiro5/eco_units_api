info:
  name: get_device_diagnostics_for_backuser
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/backuser/device_diagnostics"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
