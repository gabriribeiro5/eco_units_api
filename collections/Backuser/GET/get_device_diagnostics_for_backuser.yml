info:
  name: get_device_diagnostics_for_device_operation_supervisor
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/device_operation_supervisor/device_diagnostics"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
