info:
  name: get_device_first_auth
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/device/signature-request"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
