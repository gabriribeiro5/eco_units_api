info:
  name: get_device_config_rollback
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/device/configuration/rollback"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
