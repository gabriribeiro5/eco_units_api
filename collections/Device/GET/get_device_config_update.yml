info:
  name: get_device_config_update
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/device/configuration/update"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
