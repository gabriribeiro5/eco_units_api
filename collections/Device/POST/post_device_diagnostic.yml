info:
  name: post_device_diagnostic
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/api/device/diagnostic"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
