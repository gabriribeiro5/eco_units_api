info:
  name: post_device_config
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/device/configuration"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
