info:
  name: options_for_customer
  type: http
  seq: 4

http:
  method: options
  url: "{{base_url}}/api/customer"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
