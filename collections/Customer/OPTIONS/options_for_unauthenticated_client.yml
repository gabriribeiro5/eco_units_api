info:
  name: options_for_unauthenticated_client
  type: http
  seq: 3

http:
  method: OPTIONS
  url: "{{base_url}}/api"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
