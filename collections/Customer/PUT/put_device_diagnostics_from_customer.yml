info:
  name: put_device_diagnostics_from_customer
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base_url}}/api/customer/device_diagnostics"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
