info:
  name: post_customer_validate_token
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/api/customer/validate_token"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
