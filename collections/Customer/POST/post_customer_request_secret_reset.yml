info:
  name: post_customer_request_secret_reset
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/api/customer/request_secret_reset"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
