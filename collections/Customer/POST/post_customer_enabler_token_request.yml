info:
  name: post_customer_enabler_token_request
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/customer/enabler_token_request"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
