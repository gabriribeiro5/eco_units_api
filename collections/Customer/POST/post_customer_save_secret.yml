info:
  name: post_customer_save_secret
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/api/customer/save_secret"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
