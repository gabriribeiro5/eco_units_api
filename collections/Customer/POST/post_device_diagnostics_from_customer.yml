info:
  name: post_device_diagnostics_from_customer
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/api/customer/device_diagnostics"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
