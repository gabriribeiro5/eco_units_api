info:
  name: get_customer_login
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/customer/login"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
