info:
  name: delete_device_diagnostic
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/api/device/diagnostic"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
