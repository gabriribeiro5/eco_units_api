info:
  name: delete_device_ecosystem_state
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/api/device/ecosystem_state"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
