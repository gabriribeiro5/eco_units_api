info:
  name: options_for_backoffice_admin
  type: http
  seq: 4

http:
  method: options
  url: "{{base_url}}/api/device_operation_supervisor/admin"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
