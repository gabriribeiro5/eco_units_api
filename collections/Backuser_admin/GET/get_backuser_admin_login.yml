info:
  name: get_backuser_admin_login
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/backuser/admin/login"
  body:
    type: json
    data: |-
      {
        "agent_email": "admin@email.com",
        "agent_secret": "s3cr37@dblab73"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
