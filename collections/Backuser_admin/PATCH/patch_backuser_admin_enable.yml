info:
  name: patch_backoffice_admin_enable
  type: http
  seq: 1

http:
  method: PATCH
  url: "{{base_url}}/api/device_operation_supervisor/admin/enable"
  body:
    type: json
    data: |-
      {
        "id": "1",
        "agent_type": "backoffice_admin"
      }
  auth:
    type: bearer
    token: hash_type!admin_id!1

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
