info:
  name: options_for_unauthenticated_client
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{base_url}}/api/backuser/disable"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
