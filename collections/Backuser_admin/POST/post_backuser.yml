info:
  name: post_backuser
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/backuser"
  body:
    type: json
    data: |-
      {
        "agent_id": "1",
        "agent_name": "Gabriel Ribeiro",
        "agent_email": "gabriel@email.com",
        "agent_secret": "s0m3s3cr37",
        "confirmation_secret": "s0m3s3cr37"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
