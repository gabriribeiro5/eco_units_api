info:
  name: post_backuser_admin
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/backuser/admin"
  body:
    type: json
    data: |-
      {
        "agent_name": "admin",
        "agent_email": "admin@email.com",
        "agent_secret": "s3cr37@dblab73",
        "confirmation_secret": "s3cr37@dblab73"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
